{% extends 'base.html.twig' %}

{% block title %}Chien{% endblock %}

{% block body %}
    <h1>Chien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chien.nom }}</td>
            </tr>
            <tr>
                <th>DateNaissance</th>
                <td>{{ chien.dateNaissance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adminListeChiens') }}">back to list</a>

    <a href="{{ path('adminModifierUnChien', {'id': chien.id}) }}">edit</a>

    {{ include('chien/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscription.id }}</td>
            </tr>
            <tr>
                <th>Nb_Chien_Inscrit</th>
                <td>{{ inscription.nbChienInscrit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adminListeInscription') }}">back to list</a>

    <a href="{{ path('adminModifierUneInscription', {'id': inscription.id}) }}">edit</a>

    {{ include('inscription/_delete_form.html.twig') }}
{% endblock %}

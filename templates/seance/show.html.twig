{% extends 'base.html.twig' %}

{% block title %}Seance{% endblock %}

{% block body %}
    <h1>Seance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seance.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ seance.date }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ seance.heure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adminListeSeances') }}">back to list</a>

    <a href="{{ path('adminModifierUneSeance', {'id': seance.id}) }}">edit</a>

    {{ include('seance/_delete_form.html.twig') }}
{% endblock %}
